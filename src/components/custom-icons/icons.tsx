import React from "react";
import { Email } from "./pattern/email.icon";

export type IconProps = React.SVGProps<SVGSVGElement> & {
  size?: number;
  color?: string;
};

/**
 * Icon List
 */
export const Icon = {
  Email,
};
